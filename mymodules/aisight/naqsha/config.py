# from pathlib import Path

# city = input("Please Enter City For MapWorks: ")

# BASE_DIR = Path(__file__).parent.parent.resolve()
# DATA_DIR = BASE_DIR / "data" / city

# KML_FOLDER = DATA_DIR / "kmls"
# TIF_FOLDER = DATA_DIR / "tifs"
# CUSTOMER_FOLDER = DATA_DIR / "customers"

# population_tif = TIF_FOLDER / f"population.tif"

# sec_kml = KML_FOLDER / f"sec.kml"
# mta_kml = KML_FOLDER / f"mta.kml"
# whitespace_kml = KML_FOLDER / f"whitespace.kml"
# boundary_kml = KML_FOLDER / f"boundary.kml"


# Reading Customers Data
# customers_infile = CUSTOMER_FOLDER / f"customers.csv"

# customer_cols = {"Distributor Code": "dist_code", "Customer Code": "cust_code", "Customer Name": "cust_name", "lat": "latitude", "lng": "longitude", "Outlet Type": "outlet_type"}
# customer_columns = [ "customer_code", "customer_name", "distributor_code", "channel", "longitude", "latitude"]
# customer_columns = ["customer_code", "longitude", "latitude"]
